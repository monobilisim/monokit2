# not implemented
filesystems:
  - ext4
  - ext3
  - ext2
  - xfs
  - btrfs
  - zfs
  - fat32
  - vfat

system-load-alarm:
  enabled: true
  # cpu core count * %80 to trigger alarm by default
  limit-multiplier: 0.8
  # not implemented
  top-processes:
    load:
      enabled: true
      processes: 20

  issue-interval: 1
  issue-multiplier: 1
  issue-limit: 20

# not implemented
ram-usage-alarm:
  enabled: true

  # %90 to trigger alarm by default
  ram-limit: 90

  top-processes:
    ram:
      enabled: true
      processes: 20

  issue-interval: 1
  issue-limit: 20

# not implemented
disk-usage-alarm:
  enabled: true
  # %90 to trigger alarm by default
  disk-part-use-limit: 90
